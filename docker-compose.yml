services:
  space-weather-bot:
    build: .
    ports:
      - "8000:8000"
    environment:
      # If you need to map any environment variables, such as OPENAI_API_KEY
      - OPENAI_API_KEY=${OPENAI_API_KEY}
