services:
  space-weather-bot:
    build: .
    ports:
      - "8000:8000"
    environment:
      # If you need to map any environment variables, such as GROQ_API_KEY
      - GROQ_API_KEY=${GROQ_API_KEY}
